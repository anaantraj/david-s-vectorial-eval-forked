"""Trainable adaptation methods (soft prompt, LoRA, activation steering).

These packages hold the fitting code that runs on the cluster. The
`TransferFunction` wrappers that the harness scores live in
`vectorial_eval.transfer`.
"""
