"""Soft prompt tuning of the conditional target language model.

`train.py` is a standalone script run on the cluster; it is not imported by the
harness. `vectorial_eval.transfer.soft_prompt` loads the prompt embeddings it
writes.
"""
