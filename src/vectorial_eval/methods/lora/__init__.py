"""LoRA fine-tuning of the conditional target language model.

`train.py` is a standalone script run on the cluster; it is not imported by the
harness. `vectorial_eval.transfer.lora` loads the adapter it writes.
"""
